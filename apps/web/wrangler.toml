name = "achei-cominidade-web"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[build]
command = "bun run build"
cwd = "apps/web"

# Variáveis de ambiente para build
[env]
VITE_API_URL = "https://acheicominidade.onrender.com"

[env.production]
VITE_API_URL = "https://acheicominidade.onrender.com"
